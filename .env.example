OPENAI_API_KEY=your-key-here
OPENAI_MODEL=gpt-5-nano

# Using 3307 on the host because 3306 may already be taken by a locally installed MySQL
DB_HOST=localhost
DB_PORT=3307
DB_NAME=appdb
DB_USER=changeme
DB_PASSWORD=changeme
DB_ROOT_PASSWORD=changeme

